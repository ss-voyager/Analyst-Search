# Environment Variables for IIS Deployment
# ==========================================
#
# Note: Most configuration is done at build time or in config/voyager.json.
# Runtime environment variables are minimal.
#
# PORT and NODE_ENV are set automatically in web.config

# Base path for context path deployment (optional)
# Set this if deploying as an IIS virtual application under a path like /analyst-search/
# Must match VITE_BASE_PATH used during build
# BASE_PATH=/analyst-search/

# Build-time configuration (set in client/.env BEFORE building)
# --------------------------------------------------------------
# VITE_VOYAGER_BASE_URL - Voyager URL accessible from user browsers (for auth popups)
# VITE_BASE_PATH - Context path for virtual application deployment (e.g., /analyst-search/)

# Runtime configuration (edit config/voyager.json - NO rebuild required)
# ----------------------------------------------------------------------
# baseUrl - Voyager API endpoint for backend calls
# displayId - Solr display configuration ID
# filters - Search filter visibility settings
#
# See config/voyager.example.json for full configuration options.
# After editing config/voyager.json, restart the IIS application to apply changes.
